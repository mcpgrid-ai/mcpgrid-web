query getPageFaqs {
  # pages(filters: { Slug: { in: $slug } }) {
  #   Title
  #   Subtitle
  #   Description
  #   Slug
  # }
  frequentlyAskedQuestions {
    title
    description
  }
  # social {
  #   X
  # }
}
